# This .gitattributes file is used exclusively for Git LFS tracking.
# Only the large MongoDB backup file in this directory is handled via LFS.
assistml/mongodb.backup.gz filter=lfs diff=lfs merge=lfs -text
